# Dependências extras SOMENTE para o desafio bônus opcional (train_model.py).
# Com Poetry: instale o grupo train -> `poetry install --with train`
# Sem Poetry (compat): `pip install -r requirements.txt -r requirements_train.txt`
-r requirements.txt
scikit-learn==1.9.0